# Readable 24/7 Artists Mirror

Generated by `npm run deoptimize`. A full parallel copy of `../site/` where:

- HTML, CSS, JS, and JSON are pretty-printed with Prettier (minified theme
  bundles and inline page scripts become readable).
- SVG, XML, fonts, images, and other binaries are copied byte-for-byte.
- The original scraped runtime in `../site/` is left untouched.

These are still the compiled/bundled runtime files (webpack output), just
reformatted — not recovered original source components.

## Serve the readable copy

```bash
SITE_DIR=readable/site PORT=5275 npm run serve
```

Verify it behaves identically to the minified mirror:

```bash
BASE_URL=http://localhost:5275 npm run verify:puppeteer
```

## Last generation

- Files copied: 433
- Text files formatted: 141
- Text files left unformatted (parse fallback): 0
- Binary files copied: 292
- Formatting errors: 0
